// Tạo một hàm nhận vào một mảng và trả về mảng đã sắp xếp theo thứ tự giảm dần.
const numbers = [1, 3, 5, 7, 9, 2, 4, 6, 8];

function sapxep(mang){
    for(let i = o; i < mang.length -1 ; i++){
        let idmin = i;
        for(let j = i+1; j< mang.length; j++){
            if ()
        }
    }
}